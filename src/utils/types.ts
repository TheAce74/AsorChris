export type ServiceType = {
  id: string;
  image: string;
  title: string;
  text: string;
  linkText: string;
  linkPath: string;
};

export type ProjectWithContentType = {
  image: string;
  title: string;
  text: string;
  linkText: string;
  linkPath: string;
};
